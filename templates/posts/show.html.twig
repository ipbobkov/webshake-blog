{% extends 'base.html.twig' %}

{% block body %}
    <p><a href="{{ path('blog_posts') }}">[Список]</a>&nbsp;&nbsp;&nbsp;&nbsp;<a href="{{ path('blog_post_edit', {'slug': post.slug}) }}">[Редактировать]</a>&nbsp;&nbsp;&nbsp;&nbsp;{{ post.body }}&nbsp;&nbsp;&nbsp;&nbsp;<a href="{{ path('blog_post_delete', {'slug': post.slug}) }}">[Удалить]</a></p>
{% endblock %}